upstream edge_cluster {
{% for host in groups['workers'] %}
    server {{ hostvars[host].ansible_host }}:8080;
{% endfor %}
}

server {
    listen 80;
    location / {
        proxy_pass http://edge_cluster;
    }
}